# CacheQuake backend — Python 3.11, CPU-only PyTorch
# Pin exact minor versions so the Hugging Face Space build is reproducible.

torch>=2.3.0          # CPU-only; tested on 2.3.1+, no CUDA needed
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.27.0          # for testing FastAPI endpoints
pytest==8.2.2
